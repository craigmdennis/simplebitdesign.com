# Simple Bit Design

Served from Cloudflare Workers static assets. See `wrangler.jsonc` — note that
`snapshot/` is a captured artifact, not a build output, because the original
Ruby/gulp toolchain no longer builds.

`bin/setup`
